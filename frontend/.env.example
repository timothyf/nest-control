# API base URL used by the frontend to reach the backend
# In development the Vite proxy handles /api/* automatically (see vite.config.js).
# Override this when deploying to a different host/port.
VITE_API_BASE_URL=http://localhost:3000/api
